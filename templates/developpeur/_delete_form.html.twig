<form method="post" action="{{ path('app_developpeur_delete', {'id': developpeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ developpeur.id) }}">
    <button class="btn">Delete</button>
</form>
