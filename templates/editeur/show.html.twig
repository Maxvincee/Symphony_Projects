{% extends 'base.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item"><a href="{{ path('app_editeur_index') }}">Éditeurs</a></li>
    <li class="breadcrumb-item active" aria-current="page">{{ editeur.nom }}</li>
{% endblock %}

{% block title %}Éditeur{% endblock %}

{% block body %}
    <h1>🏢 {{ editeur.nom }}</h1>

    <div class="table-responsive mt-4">
        <table class="table">
            <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ editeur.nom }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ editeur.pays }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ editeur.description }}</td>
            </tr>
            <tr>
                <th>Site Web</th>
                <td>
                    {% if editeur.siteWeb %}
                        <a href="{{ editeur.siteWeb }}" target="_blank">{{ editeur.siteWeb }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ editeur.createdAt ? editeur.createdAt|date('d/m/Y H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Date de modification</th>
                <td>{{ editeur.updatedAt ? editeur.updatedAt|date('d/m/Y H:i') : '' }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="d-flex gap-2 mt-4">
        <a href="{{ path('app_editeur_edit', {'id': editeur.id}) }}" class="btn btn-primary">Modifier</a>
        {{ include('editeur/_delete_form.html.twig') }}
        <a href="{{ path('app_editeur_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>
{% endblock %}
