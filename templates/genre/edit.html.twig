{% extends 'base.html.twig' %}

{% block title %}Modifier le genre {{ genre.nom }}{% endblock %}

{% block body %}
    <h1>Modifier le genre {{ genre.nom }}</h1>

    {{ include('genre/_form.html.twig', {'button_label': 'Valider'}) }}

    <div class="d-flex gap-2 mt-3">
        <button type="submit" form="main-form" class="btn btn-primary">Valider</button>
        {{ include('genre/_delete_form.html.twig') }}
        <a href="{{ path('app_genre_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>
{% endblock %}
